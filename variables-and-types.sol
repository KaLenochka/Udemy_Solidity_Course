pragma solidity >= 0.7.0 < 0.9.0;

//Three main types of variables: Boolings(true/false), Integers(numbers uint/int) and Strings(sequences of characters)

contract learnVariables {
    // all our solidity code
    
    uint chocolateBar = 10;
    uint storeOwner = 300;
    bool lieDetector = true;
    string errorMessageText = 'Error! There has been a mistake!';
    
    uint wallet = 500;
    bool spend = false;
    string notifySpend = 'You have spent money!';
}
